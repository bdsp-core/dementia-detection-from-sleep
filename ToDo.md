# ToDo — SLEEP 2023 paper reproduction

Concrete next steps. Major update from previous version: the canonical
project folder `(internal project folder)/`
turned up, resolving all three Tier-1 blockers. The remaining work is
de-identification, integration, and validation.

## P0 — De-identify and ship the data

### 1. De-identify `study_criteria_table_label_V6.xlsx` ✅ DONE
- Output: `data/cohort/study_groups_deid.csv` — 22,985 × 65 (99.97% join rate via `mapping.csv` on `(PatientID, MRN_key, DateOfVisit)`).
- Keyed by `BDSPPatientID`, `HashID`, `FileNameNew` (matches AWS S3 layout).
- Date columns: `DOVshifted`, `DOBshifted`, `ShiftedDays`. All `*_dT` (days-since-PSG) columns are relative offsets and don't need shifting.
- Generated by `scripts/deidentify_dementia_cohort.py`. 6 unmatched rows retained PHI-side in `_box_inspect/`.

### 2. De-identify the per-PSG feature tables ✅ DONE
- Generated by `scripts/deidentify_feature_tables.py`. Five outputs uploaded to S3:
  - `features_macro_deid.csv` (21,223 × 24, 98.78% match)
  - `features_alpha_deid.csv` (18,955 × 43, 98.48%)
  - `features_brain_age_deid.csv` (19,294 × 485, 98.51%)
  - `features_coherence_deid.csv` (73,387 × 305, 98.51% — long form, ~3 rows per PSG)
  - `features_full_deid.csv` (22,583 × 1,071, 98.26% — wide combined matrix)
- All keyed by `BDSPPatientID`, `HashID`, `FileNameNew`, `DOVshifted`, `ShiftedDays`.
- `combined_luna_output.csv` (raw Luna spindle output keyed by `signal_<FolderName>`)
  is intentionally not auto-deidentified here — its rows are per-channel rather than
  per-PSG; reshape it via `code/02_features/combine_luna_output.ipynb` after joining.

### 3. De-identify `Neuropsychiatric Scores/` and `Dementia Validation/`
- Reports under `_box_inspect/dementia_detection_other/medical_data/{Neuropsychiatric_Scores,Dementia_Validation}/` are individual patient docx/xlsx — strip names/MRNs from filenames and headers (philter-plus or manual review).
- Output: `data/cohort/neuropsych_deid/` — adjudicated cognitive labels and source provenance.

### 4. Verify the ICD-based label tables are deidentified
- `Dementia_EDW_ICD.csv`, `Dementia_RPDR_ICD.csv`, `MCI_*_ICD.csv` — keyed by PatientID/MRN. After de-id, ship as `data/cohort/icd_dx_dates_deid.csv`.

## P1 — Integrate the canonical pipeline

### 5. Adapt `step2b/c_*ElissaCriteria*.py` paths
- Currently hard-coded to `${DEMENTIA_DATA_ROOT}/medical_data/`.
- Add a `--data-root` CLI arg (or env var `DEMENTIA_DATA_ROOT`) and document the layout in `code/01_phenotyping/README.md`.

### 6. Run the canonical notebooks end-to-end on the staged inputs
- Set `_box_inspect/dementia_detection_other/medical_data/` and `_box_inspect/dementia_detection_other/eeg_data/` as the working data root.
- Notebook order:
    1. `01_phenotyping/assign_study_groups_V3.ipynb` → `study_criteria_table_label_V6.xlsx`
    2. `02_features/compile_brain_age_features.ipynb` → `BA_features_df.csv`
    3. `02_features/combine_luna_output.ipynb` → `combined_luna_output.csv`
    4. `02_features/EEG_Coherence_Analysis.ipynb` → `coherence_df.csv`
    5. `02_features/spectral_analysis_V7.ipynb`, `spindle_analysis_V2.ipynb`, `alpha_analysis_V3.ipynb`, `compute_alpha3_alpha2_V2.ipynb` → assembles `study_features_table_v4.csv`
    6. `04_model/binary_dementia_classification_DM_v_CN_V2.ipynb` (and the MCI/DEM-MCI variants)
- Validate that headline metrics match the paper (AUROC 0.78 / 0.73 / 0.76).

### 7. Decide on shipping the trained pickles in `dementia_detection/code/`
- `DM_v_CN_LG_model.pickle`, `DM_v_CN_{LG,RF,SVM}_scores.pickle`, etc.
- Check whether these were fit on PHI-keyed or deidentified features. If deidentified, ship under `models/`.

### 8. Cross-validate against `train_classifiers.py`
- Run the from-scratch reconstruction on the same deidentified features and compare AUROC/AUPRC to the canonical notebook output. Difference > 0.02 = bug.

## P2 — Repo polish before publishing

### 9. Add `code/00_orchestration/run_all.sh`
- Bash script that runs the notebook chain (or converts to .py via `jupyter nbconvert --execute`).

### 10. Adapt `notebooks/prediction-example.ipynb`
- Replace SBOP Cox PH content with binary classification example: load a feature row, scale, predict prob, show calibrated risk.

### 11. Write `docs/methods_summary.md` + `docs/feature_dictionary.md`
- Use `code/02_features/Feature_display_name.csv` as the basis for the feature dictionary.

### 12. Tighten `requirements.txt` + add `environment.yml`
- Need: `scikit-learn`, `mne`, `pyedflib`, `nitime`, `joblib`, `tqdm`, `matplotlib`, `seaborn`, `umap-learn`, `pyrem`, `import_ipynb` (for cross-notebook imports), `openpyxl`, `xlrd`. Pin tested versions.

### 13. Document Luna installation
- `code/03_spindle/spindle_extraction_luna_HaoqiVersion.py` and `LUNAspindles/` need Luna (`http://zzz.bwh.harvard.edu/luna/`). Add install steps.

### 14. Push to a private GitHub repo first
- Once de-identification is verified by Brandon and one other reviewer, push as private. Smoke-test cloning + running on a fresh machine. Then flip to public.

### 15. Write the bdsp.io publication page
- Cite the SLEEP 2023 paper, link this repo, link the BDSP S3 buckets and credentialing flow, list the deidentified data dictionary.

## P3 — Nice-to-haves

### 16. External validation on the EMU+MRI+EEG cognition cohort
- `box:.../zz_BrainAge/(IRB number redacted).../Neuropsych_reports_EMU+MRI+EEG/E001-E060.docx` — 60 expert-adjudicated reports, IRB (IRB number redacted). Could serve as out-of-sample validation.

### 17. Companion repo for the SBOP survival paper
- Once SLEEP 2023 is shipped, mirror the layout for SBOP using `dropbox:.../SleepBasedOutcomePrediction/github-repo/` as the starting point.

### 18. Companion repo for the Spindle Optimization paper (Adra et al. 2022)
- `box:.../NoorAdra/SpindleOptimizationPaper/` has the manuscript versions; the spindle-optimization code is already in `code/03_spindle/`. Likely a small additional repo.

## Open questions (for Haoqi / Elissa)

1. Are `features_MGH_deid.csv` (SBOP github-repo) and `study_features_table_v4.csv` (`dementia_detection/eeg_data/`) the same matrix with different keys? If yes, why does the SBOP version drop coherence?
2. Were the trained pickles in `dementia_detection/code/*.pickle` fit on deidentified features? OK to ship?
3. Is there an authoritative `study_criteria_table_label_V7+`?
4. What's the recommended de-identification approach for the `Neuropsychiatric Scores/` reports — philter-plus, manual, or both?
